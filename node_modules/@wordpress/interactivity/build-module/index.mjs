// packages/interactivity/src/index.ts
import { h, cloneElement, render } from "preact";
import { batch } from "@preact/signals";
import registerDirectives, { routerRegions } from "./directives.mjs";
import { init, getRegionRootFragment, initialVdom } from "./init.mjs";
import { toVdom } from "./vdom.mjs";
import { directive } from "./hooks.mjs";
import { getNamespace } from "./namespaces.mjs";
import { parseServerData, populateServerData } from "./store.mjs";
import { proxifyState } from "./proxies/index.mjs";
import { deepReadOnly, navigationSignal } from "./utils.mjs";
import {
  store,
  getConfig,
  getServerState
} from "./store.mjs";
import { getContext, getServerContext, getElement } from "./scopes.mjs";
import {
  withScope,
  useWatch,
  useInit,
  useEffect,
  useLayoutEffect,
  useCallback,
  useMemo,
  splitTask,
  withSyncEvent
} from "./utils.mjs";
import { useState, useRef } from "preact/hooks";
if (globalThis.SCRIPT_DEBUG) {
  await import("preact/debug");
}
var requiredConsent = "I acknowledge that using private APIs means my theme or plugin will inevitably break in the next version of WordPress.";
var privateApis = (lock) => {
  if (lock === requiredConsent) {
    return {
      getRegionRootFragment,
      initialVdom,
      toVdom,
      directive,
      getNamespace,
      h,
      cloneElement,
      render,
      proxifyState,
      parseServerData,
      populateServerData,
      batch,
      routerRegions,
      deepReadOnly,
      navigationSignal
    };
  }
  throw new Error("Forbidden access.");
};
registerDirectives();
init();
export {
  getConfig,
  getContext,
  getElement,
  getServerContext,
  getServerState,
  privateApis,
  splitTask,
  store,
  useCallback,
  useEffect,
  useInit,
  useLayoutEffect,
  useMemo,
  useRef,
  useState,
  useWatch,
  withScope,
  withSyncEvent
};
//# sourceMappingURL=index.mjs.map
