/**
 * External dependencies.
 */
import { InnerBlocks } from '@wordpress/block-editor';

/**
 * The 'Save' function.
 *
 * @since x.x.x
 * @return {Element} Element to render.
 */
const Save = () => (
	<InnerBlocks.Content/>
);

export default Save;